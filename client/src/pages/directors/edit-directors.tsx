import React, { FC } from "react";
import styled from "styled-components";

const EditDirectors: FC = () => {
  return (
    <Container>
      <h1> index of movies</h1>
    </Container>
  );
};

const Container = styled.div``;

export default EditDirectors;
